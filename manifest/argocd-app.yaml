# argocd-app.yaml(edited)
apiVersion: v1
kind: Service
metadata:
  name: argocd-app-service
spec:
  selector:
    app.kubernetes.io/name: argocd-app
  type: NodePort
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: argocd-app-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: argocd-app
  template:
    metadata:
      labels:
        app.kubernetes.io/name: argocd-app
    spec:
      containers:
        - name: argocd-app-container
          image: phc9/app-with-argocd:642140a2ab560b62cd9720c0e1e583f3bf3d4a9a
          ports:
            - protocol: TCP
              containerPort: 8080
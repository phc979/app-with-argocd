# argocd-app.yaml(edited)
apiVersion: v1
kind: Service
metadata:
  name: argocd-app-service
spec:
  selector:
    app.kubernetes.io/name: argocd-app
  type: NodePort
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: argocd-app-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: argocd-app
  template:
    metadata:
      labels:
        app.kubernetes.io/name: argocd-app
    spec:
      containers:
        - name: argocd-app-container
          image: phc9/app-with-argocd:8886d316e8716ffe7b23fff8208341e9cc9d2050
          ports:
            - protocol: TCP
              containerPort: 8080